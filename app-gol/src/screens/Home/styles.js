import { StyleSheet, Platform } from "react-native";

const styles = StyleSheet.create({
  btn: {
    backgroundColor: "#fff",
    borderRadius: 6,
    width: 200,
    height: 50,
    marginLeft: '25%',
    borderWidth: 4,
    marginTop: 20
  }
});

export default styles;
